import numpy
def shiftItems(n,lst=[]):
    lst2=[]
    for lisst in lst:
        return 0

n=int(input("How many times to Shift "))
data = ["python", "r", "java", "pascal", "c", "javascript", "assembley", "html", "c++"]
shiftItems(n,data)

